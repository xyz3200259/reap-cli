import React from 'react'
import { Row } from 'antd'
import List from '../components/<%=metadata.entity.name%>/List'

export default ({
  page, dispatch, effects, showModal,editRecord
}) => {
  return (
    <Row>
      <List page={page} editRecord={editRecord} dispatch={dispatch} showModal={showModal} loading={effects['<%=metadata._name%>/search']} />
    </Row>
  )
}
