package <%=metadata.groupId%>.domain;

import org.springframework.data.mybatis.repository.support.MybatisRepository;

public interface <%=metadata.entity.name.firstUpperCase()%>Repository extends MybatisRepository<<%=metadata.entity.name.firstUpperCase()%>, String> {
  
}
